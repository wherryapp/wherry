allowBuilds:
  # tsx bundles esbuild, whose install script places the platform binary.
  # Same approval the server already grants for the same reason.
  esbuild: true
patchedDependencies:
  '@hpke/common@1.10.1': patches/@hpke__common@1.10.1.patch
